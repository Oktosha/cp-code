#include <iostream>

using namespace std;

int main() {
	int T;
	cin >> T;
	for (int test = 1; test <= T; ++test) {
		int n;
		cin >> n;
		vector<long long> a(n);
		for (int i = 0; i < n; ++i) {
			cin >> a[i];
		}
		
	}
	return 0;
}
